---
# Role default lower priority variables

# Number of seconds to delay the halt handler

halt_delay: 2

# Disable halt. This is usefull if you don't want the halt handler be executed
# (e.g. you want to delay the halt)

halt_disable: no
